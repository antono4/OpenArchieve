interface:
  display_name: "OpenArchieve"
  short_description: "Repo-specific patterns and workflows for OpenArchieve"
  default_prompt: "Use the OpenArchieve repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true